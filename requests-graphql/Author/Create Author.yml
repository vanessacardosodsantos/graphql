info:
  name: Create Author
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{url}}"
  body:
    query: |-
      mutation CreateAuthor($input: AuthorInput!) {
      	createAuthor(author: $input) {
        	firstName
          lastName
        }
      }
    variables: |-
      {
        "input": {
          "firstName":"Agatha",
          "lastName":"Christie"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
